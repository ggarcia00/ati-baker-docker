version: '3.8'

services:

  nginx:
    image: nginx-baker
    container_name: nginx-baker
    volumes:
      - /srv/docker/baker/nginx:/etc/nginx
      - /srv/docker/baker/sites:/var/www
    ports: 
      - "80:80"
    networks:
      - baker-network
    restart: always


networks:
  baker-network:
    external: true
